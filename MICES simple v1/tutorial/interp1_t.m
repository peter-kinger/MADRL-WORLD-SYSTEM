% % 定义时间序列
% tt = linspace(1800, 2500, 701);
% 
% % 定义碳排放数据（假设为一个随机生成的向量）
% emis = rand(1, 701); % 实际上应该是实际的排放数据
% 
% % 定义我们需要插值的时间点
% t = 1800;
% 
% % 插值计算在时间点 t 的碳排放量
% emis_t = interp1(tt, emis, t);
% 
% % 显示结果
% disp(['在时间点 ', num2str(t), ' 年的碳排放量为: ', num2str(emis_t)]);

% 已知数据
x = [1, 2, 4, 5];
v = [10, 20, 40, 50];

% 需要插值的点
xq = [1.5, 2.5, 3.5];

% 使用线性插值
vq = interp1(x, v, xq, 'linear');

disp(vq);
